version: '2'
services:
    taskr-mongodb:
        image: mongo:3.4.8
        ports:
            - "27017:27017"
        command: mongos --configdb taskr-mongodb-config
    taskr-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    taskr-mongodb-config:
        image: mongo:3.4.8
        container_name: taskr-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
