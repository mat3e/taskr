version: '2'
services:
    worker-mongodb:
        image: mongo:3.4.8
        ports:
            - "27017:27017"
        command: mongos --configdb worker-mongodb-config
    worker-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    worker-mongodb-config:
        image: mongo:3.4.8
        container_name: worker-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
